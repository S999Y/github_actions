name: Setup Server

on:
  workflow_dispatch:

jobs:
  Test:
    runs-on: ubuntu-latest

    steps:
      - name: Git Clone
        uses: actions/checkout@v7

      - name: Install Nodejs
        uses: actions/setup-node@v7
        with:
          node-version: "lts"

      # - name: Install Packages
      #   run: npm install

      - name: Test
        run: pwd
